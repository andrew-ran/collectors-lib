# Changelog

All notable changes to this project will be documented in this file.

The format is based on [Keep a Changelog](https://keepachangelog.com/en/1.1.0/),
and this project adheres to [Semantic Versioning](https://semver.org/spec/v2.0.0.html).

## [Unreleased]

### Security
- Root-level `.gitignore` now covers `/.env` (Docker Compose's own env file for `docker-compose.prod.yml`'s `DB_PASSWORD`/`DB_ROOT_PASSWORD` substitution) -- previously uncovered by either nested `.gitignore` (`src/backend`, `src/frontend`), which only ignore their own `.env` files. Never actually committed, caught during a post-launch security review.

### Changed
- `README.md` refreshed for the actual v1.0.0 feature set: removed the EmulationStation Desktop Edition inspiration line, broadened the feature list (books/consoles/peripherals, photo galleries, currency conversion, mobile view, setup wizard), fixed the `git clone` URL (was a `YOUR_USERNAME` placeholder), and updated the status line to link the live deployment.
- `docker-compose.prod.example.yml`/`Caddyfile.example`/`docker/nginx.prod.conf.example` reworked around CI-built images instead of building locally: `app`/`queue`/`scheduler` pull a pre-built image rather than `build:`-ing one; the old one-shot `frontend-build` container + shared `frontend_dist` volume is gone, replaced by a `web` service (nginx + the built React SPA baked in by CI, see `docker/web.Dockerfile`) with a `storage_public` named volume replacing the old backend-source-tree bind-mount into nginx.

### Added
- `.github/workflows/deploy.yml`: builds `app`/`web` images on GitHub's runners, pushes to GHCR, then deploys over SSH (`git pull` + `docker compose pull` + `up -d`) on push to `master`. Keeps build load off the VPS entirely -- see `SELF_HOSTING.md`'s "Automatic deploys (CI/CD)" section for the required secrets and why this is safe to keep in a public repo.
- `docker/web.Dockerfile`: multi-stage build producing a single image with the compiled React SPA + production nginx config baked in.
- `docker/nginx.prod.conf` (committed for real, not just as `.example`): needed because the CI build step bakes this file into the `web` image directly from the repo -- unlike `Caddyfile`/`docker-compose.prod.yml`, it can't be created locally on the server after the fact. Contains only the domain and internal routing, nothing sensitive.

### Fixed
- `.github/workflows/deploy.yml` now builds `app`/`web` for both `linux/amd64` and `linux/arm64` (via `docker/setup-qemu-action` + `docker/setup-buildx-action`) -- the first run only produced `amd64` images (GitHub runners' native arch), which the arm64 VPS could then only run under QEMU emulation rather than natively.
- `src/backend/Dockerfile`'s `composer install` now passes `--no-dev` -- the production image was installing Pest/PHPUnit/fakerphp/mockery/etc. for no reason (never used at runtime, confirmed no seeder touches Faker), which also means one fewer thing that can fail to download during a CI build.
- `docker-compose.prod.example.yml`: `app`/`queue`/`scheduler` now bind-mount `src/backend/.env` at runtime instead of relying on it being baked into the image. CI builds `app` from a clean `git checkout`, which never has a real `.env` (gitignored on purpose) -- the first CI-built deploy had `queue` crash-looping (fell back to a nonexistent SQLite default) because of exactly this. Baking real secrets into an image pushed to a *public* GHCR package would leak them to anyone who pulls it anyway, so mounting at runtime isn't just the fix, it's the only safe option.

## [1.0.0] - 2026-08-22

### Added
- Laravel 13 backend and React 19 + TypeScript + Vite frontend scaffolded (`src/backend`, `src/frontend`).
- Pest installed for backend testing.
- Migrations for all tables in `docs/DATABASE_SCHEMA.md`.
- Seeders for the two default collections and a placeholder platform list (real IGDB sync pending API access).
- `docker-compose.yml` (app, queue, frontend, nginx, db, redis) and backend `Dockerfile`.
- `.env.example` updated for MySQL/Redis service names and IGDB/Screenscraper credential placeholders.
- `/api/health` route for the Phase 0 sanity check.
- Laravel Pint config and Prettier config + scripts for the frontend.
- Eloquent models + enums (`ItemType`, `ScrapeStatus`, `Priority`, etc.) for every table.
- Admin auth via Sanctum tokens: `POST /api/auth/login`, `POST /api/auth/logout`, `GET /api/auth/me` (US-100/101/102).
- `AdminUserSeeder` (reads `ADMIN_EMAIL`/`ADMIN_PASSWORD` from `.env`).
- Item CRUD (`ItemController`): public `GET /api/items`, `GET /api/items/{item}`; auth-gated create/update/delete (US-115/116).
- Rate limiting: general `api` limiter + a stricter `login` limiter (`AppServiceProvider`).
- `IgdbService` (Twitch OAuth + Apicalypse queries) and `ScrapeItemMetadataJob` (franchise/company/genre matching included) — exercised live end-to-end 2026-08-17 (Twitch app credentials obtained, `POST /api/items` with a real `igdb_id` correctly scraped description, release year, franchise, developer/publisher, genres, and raw IGDB payload).
- Frontend auth: axios client with Bearer-token interceptor, zustand auth store (persisted to localStorage), TanStack Query hooks (`useLogin`/`useLogout`/`useCurrentAdmin`), `ProtectedRoute`, `/admin/login` and `/admin` pages, routing via react-router-dom (US-100/101/102, full stack).
- Xdebug installed in the backend `Dockerfile` (dev image only) + `src/backend/docker/xdebug.ini`, for PHPStorm step debugging.
- PHPStorm HTTP Client requests (`requests/collectors-lib.http`, `requests/http-client.env.json`) covering health check, auth login/me/logout, and item CRUD.
- US-110 (admin add-item via IGDB search), full stack: `GET /api/search/igdb` (`IgdbSearchController`, matches/creates local `Platform` rows by `igdb_id` on the fly), `GET /api/collections` (`CollectionController`), and `AdminAddItemPage.tsx` (collection picker, ~400ms debounced search, result list with cover thumbnails, platform-confirm step, US-111 "Scraping...→Ready" status per added item).
- Tailwind CSS v4 (`@tailwindcss/vite` plugin) and Framer Motion added to the frontend per `docs/TECH_STACK.md`, ahead of Phase 2's public SPA work. shadcn/ui is intentionally not bulk-installed -- components get added one at a time via its CLI as Phase 2 screens actually need them.
- Public Item View (US-001/001b/002/003/005/010), now the site's `/` homepage: `CollectionItemViewPage.tsx` shows a collection's items one at a time (cover, title, platform, genres, description, franchise, "available on" platforms, sequel/prequel/remake tags, year), with a collection-switcher dropdown, on-screen/keyboard arrow navigation, and a position indicator. Backed by `GET /api/items?collection_id=&per_page=` (index() now accepts a `per_page` override, capped at 200) and `GET /api/items/{item}` (now also eager-loads `metadata.franchise`). `Item::coverUrl()` accessor added -- resolves an admin-set primary photo, then a locally stored cover, then falls back to hot-linking IGDB's own cover image until `ImageService` exists. Deliberately out of scope for this pass: filters/sort (US-006-009), sequel/prequel tag interactivity (US-011), and the photo slider/lightbox (US-012).
- Filters, sort, URL state, and empty/loading states for the Item View (US-006/006a/007/008/008b/008c/009): Platform/Genre/Series filter tags (grayed out when the current collection has no values for that dimension), a sort dropdown (Newest/Oldest added, A→Z, Z→A -- "added" means `acquired_date`, not `created_at`), a shared `Dropdown`/`DropdownItem` component (extracted from the collection switcher, same 4-row-then-scroll behavior everywhere), item count, and distinct "no items match these filters" (with a Reset action) vs "collection is genuinely empty" states. Selection is stored in the URL (`?collection=&platform=&genre=&franchise=&sort=`) via `useSearchParams`, so a view is bookmarkable/shareable; changing any of them remounts the item browser (via a composed `key`) to reset back to item 1, instead of an effect+setState. Backend: `GET /api/items/filter-options` (scoped to the current collection) and `ItemController::index()` now accepts `platform_id`/`genre_id`/`franchise_id`/`sort`. Wishlist's Most/Least-wanted and Price sort options are deferred until the Wishlist card fields (US-020/021) exist to fetch that data from.
- Wishlist card fields and priority/acquisition badges (US-020/021/022/023): `ItemCard` now shows condition preference, edition note, and estimated price (new/used) for items in a Wishlist-type collection, plus a click-to-expand priority badge (ice cube/orange slime/burning meteor icon over the cover, expanding to a pill with the exact desire score %). Items that started in the Wishlist and have since moved to a regular collection (`wishlist_detail.received === true`) instead show a Gifted/Purchased acquisition badge in the same card position -- a registered gifter's avatar (hover tooltip with their name, click-to-expand pill with the thank-you note), a generic gift-box icon for a gift with no gifter on file, or a wallet+checkmark icon for a self-purchase (both tooltip-only, not interactive). Backend: `WishlistDetail::priority()` is now an appended Eloquent accessor (was a plain, unserialized method) derived from `desire_score`; `ItemController::show()` eager-loads `wishlistDetail.gifter`. Currency display (US-170/171) isn't wired up yet -- prices render as plain numbers. Untested against real acquisition-badge data, since US-151 (mark received) doesn't exist yet to produce it.
- Related-game tag interactivity (US-011): sequel/prequel/remake tags now show a comic-style tooltip on hover ("не в коллекции" if the game isn't owned or wishlisted anywhere, "Wishlisted" if it is), a green checkmark if it's owned, and are clickable when owned/wishlisted -- clicking jumps straight to that game's own card, in its own collection. Backend batch-matches each tag's IGDB id against local items in one query (`ItemController::enrichRelatedGames()`) rather than one query per tag.
  - **Known limitation, by design:** IGDB's API has no direct "sequel"/"prequel" relation between games -- only franchise membership and a release date. "Sequels"/"prequels" are therefore *approximated*: every other game in the same IGDB franchise, split into earlier/later by release date relative to the current game. This also catches spin-offs and side entries from the same franchise, not just direct numbered sequels. Remakes/remasters are exact (IGDB does have those as real fields). Planned to be revisited -- see `docs/tz/TECH_DEBT.md`.
- The Related row is now capped by default to the 2 closest prequels + 2 closest sequels + anything already owned + all remakes, with a "Show more" link revealing the full list -- a long-running franchise's full sequel/prequel list was stretching the card considerably. Still a stopgap, not a real layout fix -- see `docs/tz/TECH_DEBT.md`.
- Mobile view (US-030-037): below 768px, the collection/wishlist now switches from desktop's one-item-at-a-time Item View to a mobile-only Table View (default) or Item View (full-detail cards stacked vertically), toggled from the sort/filter bar. Table View shows Item Number + Title (cover as a background, fading left-to-right) plus two more columns depending on collection type and active sort -- Platform/Genre for regular collections, Platform+Desire or Desire+Price for the Wishlist. Tapping a row opens a popup with the same fields/order as Item View. The sort/filter bar becomes sticky on scroll (the collection name/switcher above it doesn't); a floating scroll-to-top button appears once scrolled. Backend: `GET /api/items` now also returns platform/genres/cover_url/a compact wishlist summary per row, so Table View doesn't need a per-row request. Known gaps -- see `docs/tz/TECH_DEBT.md`: Price/Most-wanted sort (and their table columns) aren't wired up yet; mobile Item View fetches each card's detail individually rather than being lazy-loaded.
- Currency selector and converted wishlist prices (US-170/171/172): a flag+code pill (top of the interface, next to the collection switcher) lets a visitor pick EUR/USD/RUB/PLN/RSD; every displayed wishlist price recalculates immediately, rounded per US-171's per-currency rule (nearest whole number for EUR/USD/PLN, nearest 10 for RUB, nearest 100 for RSD) with a "~" prefix (e.g. "~1 270 ₽"). Selection persists to localStorage, not the URL -- a personal display preference, not shareable view state. Backend: `ExchangeRateSyncJob` fetches and caches EUR-based rates, scheduled monthly (`routes/console.php`); `GET /api/exchange-rates` serves the cached rates (a currency is only offered once it actually has a cached rate -- same disabled-option pattern as US-006a's filters). Added a `scheduler` service to `docker-compose.yml` (`php artisan schedule:work`) -- nothing was previously running Laravel's scheduler at all, so `Schedule::job()->monthly()` would otherwise just sit inert. `exchange_rates` is empty on a fresh install; run `App\Jobs\ExchangeRateSyncJob::dispatch()` once via tinker instead of waiting a month for the first real rates.
  - **Source changed from frankfurter.app to open.er-api.com** (both free, no API key): frankfurter.app -- this project's original example source -- is ECB-sourced and simply doesn't publish rates for RUB or RSD at all, two of the five currencies US-170 requires. Confirmed live 2026-08-21 before building against it.
- Site title/description settings (US-180): the admin can set a Site Title and Site Description from a new form on the admin dashboard (`SiteSettingsAdmin`), stored in a generic key-value `settings` table (`GET /api/settings` public with defaults, `PUT /api/settings` admin-only). `useSiteMeta()` applies these to `document.title`/`<meta name="description">` once the SPA loads. Defaults to "Collectors Lib" / its tagline until set.
  - **US-181 (bot/search-engine-visible previews) is deferred, not included here:** the above only reaches visitors whose browser executes JavaScript. Chat-app link-preview bots and search-engine crawlers still see `index.html`'s static fallback, since nothing server-templates the HTML document yet -- see `docs/tz/TECH_DEBT.md`.
- Admin edit item form (US-112/113/114/115/116/123): `AdminEditItemPage.tsx`, a WordPress-post-edit-style layout (main column: Title/Description/Genres/Franchise/Developer/Publisher, all autocomplete-assisted via native `<datalist>`; sidebar: Platform, Collection, Acquired date+precision, Purchase price, Notes, scrape status, Save/Delete). A field the admin has actually changed shows a "(manually edited)" indicator and is skipped by future re-scrapes. Reachable via a small Edit link on the public Item View when an admin token is present. Backend: `ItemController::update()` now also accepts optional metadata fields and diffs each one against its current value to decide whether to flag it as manually overridden (only an actual change counts -- the edit form always resubmits every field, so "present in the request" alone would over-flag untouched fields); `POST /api/items/{item}/rescrape` (US-113) triggers `ScrapeItemMetadataJob`, which now actually reads `manual_overrides` before overwriting title/description/franchise/developer/publisher/genres/remakes/remasters (previously it always overwrote all of these, regardless of any manual edits -- a latent bug fixed as part of this). New `GET /api/platforms`, `/api/genres`, `/api/franchises`, `/api/companies` (admin-only) back the form's dropdowns/autocomplete.
- Admin item list (`/admin/items`, `AdminItemsPage.tsx`): search-by-title + collection filter, table linking straight into the edit form -- no more needing an item's id or hunting through the public site to edit something. Backend: `ItemController::index()` gained an optional `q` title-search param, combinable with the existing `collection_id` filter (omitting it already meant "every collection").
- Minimal Tailwind design pass across every admin screen (login, dashboard, add-item, edit-item, items list, site settings): shared classes in `components/Admin/adminUi.ts` give Save/Delete real buttons (were plain text links), visible bordered+shadowed inputs, and white-on-gray card grouping -- admin had been entirely unstyled inline markup since Phase 1. The public Item View's Edit link is now a small pill pinned to the card's top-right corner instead of sitting inline next to the title. Editing an item now returns to wherever the admin came from after Save/Delete (the collection view, or `/admin/items`) instead of always landing on `/admin`.
- Collections admin CRUD (US-130/131/132/133): `CollectionsAdmin.tsx` on the admin dashboard -- a list with inline rename, a wishlist-type checkbox toggle, delete, and an "add collection" form. Backend: `POST`/`PUT`/`DELETE /api/collections{/id}` on `CollectionController`, all admin-only. New collections auto-slug from their name, appending `-2`/`-3`/... on a collision. The two default collections ("My Collection"/"Wishlist") can't be renamed or deleted -- description and the wishlist-type flag stay editable on them, only the name (and its derived slug) is locked. Deleting a non-empty collection is also rejected with a clear message: `items.collection_id` is `cascadeOnDelete()`, so without this guard, deleting a collection here would have silently deleted every item inside it too -- caught while building this, not a hypothetical.
- Gifter profile CRUD (US-160/161): `GiftersAdmin.tsx` on the admin dashboard -- a list with avatar thumbnails, inline rename with optional avatar replace, delete, and an "add gifter" form. Backend: `GET`/`POST /api/gifters`, `POST /api/gifters/{gifter}` (Laravel `_method=PUT` spoofing, needed for multipart file uploads), `DELETE /api/gifters/{gifter}`, all admin-only -- gifters are never listed publicly, only surfaced per-item via `wishlist_detail.gifter`. Uploaded avatars are re-encoded to WebP via Imagick (400px cap, quality 85, first-frame-only for animated GIFs), matching the WebP approach already planned for item photos. Deleting a gifter used on a past acquisition is safe and unguarded, unlike collections -- `wishlist_details.gifter_id` is `nullOnDelete()`, not cascading.
- Item photo management (US-117/118/119/120): `ItemPhotoManager.tsx` on the edit-item admin page -- a grid-of-tiles view (WP media library / Shopify product-photos pattern), a dropzone tile supporting drag-and-drop or multi-select upload, native-HTML5-drag reordering, a hover-revealed "set primary" star, and a hover-revealed delete icon. The IGDB cover is always shown as its own permanent tile (never deletable/draggable) and carries the "Primary" badge whenever no uploaded photo overrides it. Backend: `POST /api/items/{item}/photos` (multi-file, validated for real MIME type + a 10MB max before processing), `PUT .../photos/reorder`, `PUT .../photos/{photo}/primary`, `DELETE .../photos/{photo}`, all admin-only. Uploads are re-encoded to WebP via a new shared `ImageService` (1600px cap, quality 85, per the TZ's Q6 answer) -- extracted from `GifterController`'s original Imagick code so both features share one conversion path instead of duplicating it. Also fixed a real latent bug found while wiring this up: `Item::coverUrl()`/`primaryImagePath()` were returning the raw stored disk path instead of a resolved URL -- harmless until now since `item_photos` was always empty and `cover_image_path` was never set by anything, but every item card would have silently failed to load its cover the moment the first photo was ever uploaded.
- Wishlist admin fields and mark-as-received flow (US-150/151/162/163): `WishlistAdminPanel.tsx` on the edit-item admin page, shown only while the item's current collection is wishlist-type. A "Wishlist details" form (condition preference, edition note, price estimates, desire-score slider) saves independently of the main item form via `PUT /api/items/{item}/wishlist-detail`. A "Mark as received" form -- Gifted/Self-purchased toggle with progressive disclosure, a gifter picker or one-off name (mutually exclusive), thank-you note, price paid, received date, destination collection (defaults to the non-wishlist default collection) -- confirms via `POST /api/items/{item}/mark-received`, which sets the acquisition fields, copies `price_paid` onto `items.purchase_price`, and moves the item into the chosen collection (same mechanism as US-114's collection picker). The panel disappears on its own afterward, since the item's collection is no longer wishlist-type. Also fixed another instance of the raw-path-instead-of-URL bug from the item photos work above: `GifterRef.avatar_path` was being rendered directly as an `<img src>` on the public Item View's acquisition badge -- harmless until this feature made it possible to actually assign a real gifter (with an avatar) to an item for the first time. Added `avatar_url` to the type and switched the badge to use it.
- Backend test coverage: enabled `RefreshDatabase` for the Feature test suite (was scaffolded but commented out) and added factories for `AdminUser`, `Collection`, `Item`, `Gifter`, and `ItemPhoto`. New Feature tests target the riskiest logic across recent features rather than every CRUD happy path -- auth (including that logout revokes only the current token), the collection delete/rename guards, the gifter delete's `nullOnDelete` behavior (and a real Imagick/WebP round-trip via a faked upload), item photo ownership checks across items, the mark-received flow's gifter-vs-one-off-name precedence, and the edit form's diff-based (not presence-based) manual-override flagging.
- Frontend test coverage: added Vitest + jsdom + React Testing Library (`vite.config.ts`'s `test` block, `src/test/setup.ts`, `npm test`/`npm run test:watch`). Tests cover `roundForCurrency`/`formatThousands` (US-171), `resolveReturnTo` (extracted into its own module out of `AdminEditItemPage.tsx`, since a component-exporting file can't also export a plain function under `react-refresh/only-export-components`), `useIsMobile`, and `CurrencyProvider`/`useCurrency`'s full conversion-and-rounding pipeline against a mocked `apiClient`.
- Photo slider + lightbox (US-012): the public Item View's cover image area (`components/PhotoGallery.tsx`, `components/galleryImages.ts`) now shows a gallery built from the IGDB cover plus every admin-uploaded photo (US-117-120), same order as `ItemPhotoManager.tsx`'s tiles. A single-image item is unchanged (no interaction, no dots); once there's more than one image, moving the mouse (desktop) or a finger (touch) across the cover previews the others by cursor position, small dots show how many exist, and clicking opens a Lightbox: item-card-width, left/right arrow buttons plus physical keyboard arrow keys, infinite-looping navigation, a centered thumbnail strip that auto-scrolls the active thumbnail into view, and Escape/click-outside to close. Built from scratch (no new npm dependency) rather than pulling in a slider/lightbox library, per `ARCHITECTURE.md`'s note to evaluate one -- see `docs/tz/TECH_DEBT.md` for the reasoning.
- Add a book by ISBN or manually (US-121): a new "Add book" admin page (`AdminAddBookPage.tsx`) alongside the existing IGDB game-search flow. Entering an ISBN and clicking "Lookup" fetches title/author/publisher/year/cover from OpenLibrary (`OpenLibraryService::findByIsbn()`, a single exact bibkeys lookup -- not a fuzzy multi-result search like IGDB's `search.json`) and pre-fills the form; every field stays editable, and a lookup miss (404) just means filling them in by hand instead of blocking the add. Backend: `GET /api/search/open-library` (admin-only, mirrors `/search/igdb`'s route shape); `ItemController::store()` now also writes `author`/`description`/`publisher`/`release_year` straight into `item_metadata` in the same request when present, instead of only ever creating a bare `items` row -- a book's data is already fully resolved client-side by submit time, unlike a game add, which only ever sends an `igdb_id` for `ScrapeItemMetadataJob` to fill in asynchronously afterwards. New `item_metadata.author` column, since `developer` doesn't semantically fit a book (`publisher` and the already-generic `items.cover_image_url` are reused as-is).
- Add a console or peripheral manually (US-122): a new "Add console/peripheral" admin page (`AdminAddManualItemPage.tsx`) -- collection picker, a Console/Peripheral type toggle, title, and an optional platform (reusing the same `usePlatformOptions()` dictionary the edit form's autocomplete already uses). No external metadata source at all, unlike the game/book flows -- IGDB's console/peripheral data is too limited to be worth scraping, per REQUIREMENTS.md's own note. Needed no backend changes whatsoever: `ItemController::store()` already accepted `console`/`peripheral` as valid `type` values and every field this form sends -- the only gap was a frontend entry point.
- `php artisan setup` first-run wizard: a new interactive Artisan command (`App\Console\Commands\SetupCommand`) replacing the old "hand-edit .env, then `php artisan migrate --seed`" bootstrap path. Generates `APP_KEY` if missing, runs migrations, links storage, seeds the default collections/platform list, then walks through IGDB credentials (saved to `.env`, skippable) and the admin account (email + password, hashed straight into the database -- not read back out of `.env` like the old `AdminUserSeeder` path, so the password never sits in plaintext on disk). Safe to re-run any time (e.g. to rotate the admin password).
- Self-hosting guide (`SELF_HOSTING.md`, repo root -- published, unlike the internal `docs/tz/` planning docs): local/LAN quick start via `php artisan setup`, what a public deployment needs on top of the dev `docker-compose.yml` (production frontend build, an HTTPS reverse proxy, not publishing `db`/`redis` ports, changing default passwords), the `.ru`/`.su` Let's Encrypt restriction and Gosuslugi-CA alternative, a note on Cloudflare's Russian-ISP throttling as a CDN trade-off, a daily backup cron example, and an update procedure. Also added `docker-compose.prod.example.yml`, `Caddyfile.example`, and `docker/nginx.prod.conf.example` as a worked example of the production topology the guide describes -- illustrative, not live-tested against a real domain/certificate issuance. `README.md`'s Tech Stack (was stale at Laravel 11/React 18) and Quick Start (was "Coming Soon") updated to match current reality and link the new guide.

### Fixed
- `Dockerfile` was missing the `redis` PHP extension, breaking anything touching cache/queue/rate-limiting ("Class \"Redis\" not found").
- nginx dotfile-deny rule was blocking Vite's `/node_modules/.vite/deps/...` cache requests (403) — removed, see `docker/nginx.conf`.
- Docker nginx moved from host port 80 → 8080 → 8090. Port 80 is taken by a native Homebrew nginx, and port 8080 turned out to be taken by that same native nginx too (serving other local vhosts, e.g. `Fortis`) — requests to `:8080` were silently answered by it instead of our container. `docker-compose.yml` and `.env`/`.env.example` (`APP_URL`, `FRONTEND_URL`) updated to `collectors-lib.test:8090`.
- `db` service host port moved from 3306 → 3307 (3306 is already used by another local MySQL install). Internal `DB_PORT` in `.env` stays 3306 (container-to-container, unaffected).
- `ScrapeItemMetadataJob` never matched/attached genres (`item_genres` stayed empty despite `igdb_raw.genres` having data) — added `matchGenres()` (mirrors `matchFranchise()`) and `item->genres()->sync(...)`.
- `ScrapeItemMetadataJob` only overwrote `title` when it was empty (`?:`), which never happens since `title` is required at creation — title is now always overwritten with IGDB's name once an item has an `igdb_id`, since IGDB is the authoritative source.
- `IgdbSearchController::matchPlatform()` matched existing platforms by `igdb_id` only, colliding on the unique `slug` constraint against `PlatformSeeder`'s pre-seeded rows (which intentionally have `igdb_id = null`) — e.g. searching a PS4 title 500'd on a duplicate `playstation-4` slug. Now also falls back to matching by slug and backfills `igdb_id` on the existing row instead of trying to insert a duplicate.
- `AdminAddItemPage.tsx` silently swallowed search errors (just stopped showing "Searching..." with no result and no error) — added error display and a "No results." state.
- CI's backend job failed on `cp .env.testing.example .env.testing` -- that file never existed (`.github/workflows/ci.yml` was written during Phase 0 docs, before there was a real test setup to point it at). Added `src/backend/.env.testing.example`, wired to the CI workflow's `mysql` service and `array`/`sync` drivers (no Redis service defined in CI).
- `.github/workflows/ci.yml` bumped `actions/checkout`/`actions/setup-node` v4 → v7 (v4 still runs on the Node 20 runtime GitHub is removing from Actions runners on 2026-09-16) and the frontend job's `node-version` 20 → 22, matching the Node version used everywhere else in the project (Docker, local dev).
- `ScrapeItemMetadataJob` populated the "sequels" field from IGDB's `similar_games` (a fuzzy similarity match, not real sequel data) and never populated "prequels" at all. Both are now derived from same-franchise games split by release date -- see the Related-game tag entry above for the approximation this introduces instead.
- The collection switcher's dropdown opened underneath the mobile sort/filter bar once it went sticky (the sticky bar's own stacking context sat on top of it). Wrapped the switcher in its own elevated stacking context (`z-30` vs. the bar's `z-20`).
- Clicking a Related tag pointing at an already-owned/wishlisted game did nothing on mobile -- the `?item=` jump was only ever wired up for desktop's single-active-card view. Mobile Table/Item View now scroll the target row/card into view instead (there's no single active card to swap to on mobile, since both views show every item at once).
- `PUT /api/gifters/{gifter}` (rename/avatar-replace, US-161) was registered as `Route::post()`, reasoning that the frontend sends a real POST with `_method=PUT` spoofing (needed so PHP populates `$_FILES` on a multipart body). That reasoning was backwards: Laravel's method-override support converts the *routed* method to PUT before matching, so the route itself must be declared `Route::put()` -- a route declared as POST never matches a spoofed PUT request at all. Every real rename or avatar replace from `GiftersAdmin.tsx` has been failing with 405 since the Gifters feature shipped; caught immediately by `GifterControllerTest`'s first real run.
- Frontend CI (`npm ci`) failed after the Vitest test-infra commit: `package.json` gained `vitest`/`jsdom`/`@testing-library/*` as devDependencies, but `package-lock.json` was never regenerated to match (this sandbox's npm registry access is blocked for installs, so the lock file could only be hand-authored alongside `package.json`, not actually resolved). Fixed by running a real `npm install` locally to regenerate the lock file.
